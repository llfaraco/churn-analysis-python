{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# 📉 Churn Analysis – EDA em Python\n",
    "\n",
    "Análise exploratória de churn com foco em comportamento, risco e padrões de cancelamento."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Imports principais e leitura do dataset\n",
    "import pandas as pd\n",
    "import numpy as np\n",
    "import matplotlib.pyplot as plt\n",
    "import seaborn as sns\n",
    "\n",
    "sns.set(style=\"whitegrid\")\n",
    "plt.rcParams['figure.figsize'] = (10, 6)\n",
    "\n",
    "df = pd.read_csv(\"../data/churn_clientes.csv\")\n",
    "df.head()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Estrutura geral do dataset\n",
    "df.info()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Estatísticas descritivas (numéricas e categóricas)\n",
    "df.describe(include=\"all\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Cálculo da taxa geral de churn\n",
    "churn_rate = df['churn'].mean()\n",
    "print(f\"Taxa geral de churn: {churn_rate:.2%}\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Taxa de churn por segmento\n",
    "segment_churn = df.groupby('segmento')['churn'].mean().sort_values(ascending=False)\n",
    "segment_churn"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Visualização da taxa de churn por segmento\n",
    "sns.barplot(x=segment_churn.index, y=segment_churn.values)\n",
    "plt.title('Taxa de churn por segmento')\n",
    "plt.ylabel('Taxa de churn')\n",
    "plt.xlabel('Segmento')\n",
    "plt.xticks(rotation=0)\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Taxa de churn por canal\n",
    "canal_churn = df.groupby('canal')['churn'].mean().sort_values(ascending=False)\n",
    "canal_churn"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Visualização da taxa de churn por canal\n",
    "sns.barplot(x=canal_churn.index, y=canal_churn.values)\n",
    "plt.title('Taxa de churn por canal')\n",
    "plt.ylabel('Taxa de churn')\n",
    "plt.xlabel('Canal')\n",
    "plt.xticks(rotation=0)\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Relação entre atrasos de pagamento e churn\n",
    "sns.boxplot(x='churn', y='atrasos_pagamento', data=df)\n",
    "plt.title('Atrasos de pagamento por status de churn')\n",
    "plt.xlabel('Churn (0 = ativo, 1 = cancelado)')\n",
    "plt.ylabel('Atrasos de pagamento')\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Matriz de correlação entre variáveis numéricas\n",
    "corr = df[['idade','tempo_de_casa_meses','ticket_medio','atrasos_pagamento','atendimentos_suporte','churn']].corr()\n",
    "\n",
    "sns.heatmap(corr, annot=True, cmap='Blues')\n",
    "plt.title('Matriz de correlação')\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## ✅ Conclusões\n",
    "\n",
    "- Clientes com maior número de atrasos de pagamento apresentam maior probabilidade de churn.\n",
    "- Segmentos com menor ticket médio concentram proporcionalmente mais cancelamentos.\n",
    "- Queda de uso do produto está associada a maior risco de cancelamento.\n",
    "- Clientes com muitos atendimentos de suporte tendem a cancelar mais.\n",
    "\n",
    "Este tipo de análise pode apoiar times de Customer Success, Marketing e Produto a reduzir churn e aumentar a previsibilidade de receita."
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.10"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}
